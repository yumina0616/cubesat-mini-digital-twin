"""
orbital_constants.py - 궤도 시뮬레이션에 사용되는 물리 상수 모음

설명:
    이 파일은 일종의 "설정 파일"입니다.
    물리 상수를 코드 곳곳에 하드코딩하면 나중에 수정하기 어렵기 때문에
    한 곳에 모아서 관리합니다.
"""

# ── 지구 물리 상수 ────────────────────────────────────────────────

# 표준 중력 가속도 (m/s²)
# 지구 표면에서 물체가 자유낙하할 때 가속도
G_SURFACE = 9.80665

# 지구 반지름 (m) - 평균값 사용
EARTH_RADIUS = 6_371_000.0  # 6,371 km

# 지구의 표준 중력 파라미터 μ = G * M_earth (m³/s²)
# G(만유인력 상수) × M(지구 질량)을 곱한 값
# 이 값 하나로 모든 궤도 계산이 가능 → G와 M을 따로 쓸 필요 없음
MU_EARTH = 3.986004418e14   # 3.986 × 10^14 m³/s²

# ── CubeSat 기본 파라미터 ─────────────────────────────────────────

# 1U CubeSat 기준 질량 (kg)
CUBESAT_MASS = 1.33

# ── 궤도 파라미터 (LEO 기준) ──────────────────────────────────────

# 저궤도(LEO) 고도 범위 (m)
LEO_ALTITUDE_MIN = 200_000   # 200 km
LEO_ALTITUDE_MAX = 2_000_000  # 2,000 km

# 기본 시뮬레이션 고도 (m) - 국제우주정거장 ISS와 비슷한 고도
DEFAULT_ALTITUDE = 400_000   # 400 km

# ── 시뮬레이션 설정 ──────────────────────────────────────────────

# 기본 시뮬레이션 시간 스텝 (s)
# 너무 크면 수치 오차 증가, 너무 작으면 계산 느림 → 1초가 적당
DT = 1.0

# 기본 시뮬레이션 시간 (s) - 궤도 1바퀴 반 정도
# 400km 고도에서 1바퀴 ≈ 92분 = 5,520초
DEFAULT_SIM_TIME = 6000.0  # 100분